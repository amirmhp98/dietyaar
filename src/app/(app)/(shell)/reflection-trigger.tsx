'use client';

/**
 * STUB — replaced by the Today/History agent (task 8.2): requests today's
 * reflection on the first authenticated visit of the local day from any
 * product page, without blocking the page.
 */
export function ReflectionTrigger(_props: { timeZone: string }) {
  return null;
}
