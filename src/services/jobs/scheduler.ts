/**
 * STUB — replaced by the jobs agent (task 4.1): JobLock lease loop, task
 * cadence, `runJobsNow()` for `after()`.
 */
export async function startScheduler(): Promise<void> {}
export async function runJobsNow(_taskName?: string): Promise<void> {}
