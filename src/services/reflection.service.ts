/**
 * STUB — replaced by the day/reflection agent (task 8.1). Meal, plan and
 * completeness mutations call `markStaleIfNeeded` with the date they touched.
 */
export async function markStaleIfNeeded(_ownerId: string, _affectedDate: string): Promise<void> {}
