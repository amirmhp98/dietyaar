/**
 * Every account counts its days in this zone (decision 022): the `lib/time`
 * functions stay zone-parameterised and this is the value callers pass them.
 */
export const APP_TIME_ZONE = 'Asia/Dubai';
